{
  "_schema": "backlog-archive/io-v1",
  "_note": [
    "Cold store for backlog items that landed in undated.",
    "Written by tools/session/archive_designs.js; read by backlog_query.js --full.",
    "Records here are FROZEN history. The live index is docs/development/backlog.json,",
    "which points at this file via each item's `archived` field. To amend a landed",
    "item's prose, edit it HERE — the hot file no longer carries a copy."
  ],
  "bucket": "undated",
  "records": {
    "BL-038": {
      "completion_note": "Folded into BL-039; inter-body market coupling lands with convoy delivery (credit_arrived_convoys injects into market.supply). Complete 2026-06-17."
    },
    "BL-039": {
      "completion_note": "Supply convoys, logistics, and inter-body market coupling (R1-R9) complete — supply_system.{hpp,cpp}, supply_lens.lua golden. 2026-06-17."
    },
    "BL-040": {
      "design": "**Scope: v0.2 (post-prototype) -- deferred, NOT implemented.** The prototype targets v0.1.0 (ROADMAP); full-set deposit authoring is outside the current arc, so per the standing scope rule it is not built now.\n\n**Direction settled (2026-06-15); mechanics settle with the v0.2 tile-generation brief.** A **seeded per-resource rarity scalar** -- a decimal in [0, 1] (0 = trace/absent, 1 = near-universal ambient), **raw-tier resources only** (refined and product goods are made, not mined, so carry no deposits). The scalar **modulates deposit frequency and magnitude** on top of the existing terrain affinity, so a low scalar keeps a good sparse even on affine terrain and a high one approaches the every-tile ambient floor; it is **seeded** so a campaign's exact distribution varies while the rarity *ordering* (consistent with each good's RESOURCES.md base-price rarity) stays stable. The generation *mechanics* that consume the scalar are the tile-generation brief (BL-051), the same v0.2 pass. Settled into docs/economy/RESOURCES.md § Deposit rarity & scarcity and docs/generation/TILE_GENERATION.md § Deferred. Touches the deposit pass in src/world/tile_generation.cpp (data-only -- the deposit arrays already carry full enum width). src/-changing -> a headless deposit-distribution audit requirement at promotion. Status stays design-owed: direction is settled but the item is not promote-ready until the v0.2 pass is scheduled.\n\n**Record note (2026-07-04 reconciliation).** The 2026-07-01 'pointer repaired' note was wrong -- a full BL-040 body *did* survive in BACKLOG.md (richer than the reconstruction it replaced). That body is folded in above and tombstoned in BACKLOG.md.\n\n### Corrected 2026-07-21 (Sprint 1 procgen review)\nFound already **implemented**, not design-owed: `tile_generation.cpp` (BL-040 markers ~line 535, 645-657, 899-919) carries the full seeded-rarity-scalar deposit pass on an independent `rare_rng` stream exactly as designed above, and `docs/economy/RESOURCES.md` § Deposit rarity & scarcity documents it as implemented, guarded by the `world_audit` headless harness (R1). Flipped `design-owed` → `complete`; this was pure backlog bookkeeping drift, no new work done."
    },
    "BL-045": {
      "completion_note": "Folded into BL-039; logistics cost constants in economy.lua, loaded via recipe_registry. Complete 2026-06-17."
    },
    "BL-046": {
      "design": "Umbrella index only -- no design of its own; the design lives in the child items it decomposed into (2026-06-15). [Pointer repaired 2026-07-04 reconciliation: the prior @BACKLOG.md pointer dangled -- the BACKLOG.md body was removed without a tombstone when the item closed.]"
    },
    "BL-051": {
      "design": "**Status:** designed (settled 2026-07-21, Sprint 1; parked, F). No BACKLOG.md body was preserved — see this item's `summary`. Generation-mechanics passes only (the deposit-data half moved to BL-040). Open: smooth band transitions + coastline refinement (cosmetic, deferred); speculative orbital-derivation + tectonic landforms (beyond v0.1.0). Authority docs/generation/TILE_GENERATION.md.\n\n### Settled 2026-07-21 (Ben, Sprint 1 procgen review)\n**Pipeline shape: sibling passes, not a growing monolith.** Ben's call for the whole procgen cluster this sprint: the six-pass `generate_body_tiles` pipeline stays fixed and stable; every future generation concern — this item's coastline/band smoothing, BL-040's rarity scalar (already this shape), BL-167's atmosphere/evolution model, any future tectonics — lands as its own **sibling pass** (a separate file/function reading the shared `generation_record`, invoked after the six-pass core), the same convention this sprint's BL-170 river-edge trace established. New passes compose; the core pipeline doesn't bloat. This is now the standing architecture decision for TILE_GENERATION.md extensions generally, not just this item.\n\n**Scope split within this item (unchanged, just reconfirmed under the new convention).** Smooth band transitions + coastline refinement are buildable, cosmetic, low-risk sibling passes whenever scheduled — no new design work needed, just implementation. Orbital-derivation and tectonic landforms remain genuinely design-owed and speculative; do not implement speculatively. Item stays parked (F) as a whole since the speculative half isn't scheduled, but the buildable half is now unambiguously promote-ready as a sibling pass whenever picked up.\n\nCLOSED 2026-08-07 settled-in-design (cut audit, Ben approved). Split to nothing: deposits moved to BL-040 (complete), speculative tectonics subsumed by BL-210, and the residue is cosmetic band/coastline polish on a pipeline the Sprint 1 review froze."
    },
    "BL-054": {
      "design": "**Status:** design-owed — shape settled 2026-07-08, the full design still owed; parked/out-of-scope, not promote-ready. Settling the structure now (per BL-087's model) so it's ready to pick up once nations move from generation-only to a system.\n\n**Shape.** Four coupled parts, all deterministic and tick-driven like the rest of `world/*`:\n\n1. **Nation SYSTEM.** A nation gets simple per-tick economic state — tax rate (levied on corp production/trade within its territory), a licence gate (a corp needs a licence to build/operate in a nation, cost scales with nation character), and a coarse infrastructure scalar (modulates production/logistics efficiency, generated once, degrading/improving slowly). War is a state flag between nation pairs affecting licence issuance and infrastructure decay, not a combat system — actual Conflict mechanics live elsewhere.\n2. **Sentiment graph.** A small per-nation-pair scalar (-1..1) seeded at generation from shared borders/resource competition, drifting slowly from corp activity (heavy extraction in a nation nudges its sentiment toward the extracting corp's home nation). Read-only input to licence cost/availability; no diplomacy UI for the prototype's follow-on scope.\n3. **Historical fragmentation.** Exclaves and disputed zones are a **generation-time** decoration on top of BL-053's Voronoi/merge output — after merge, some tiles are deliberately left owned by a non-contiguous or disputed neighbour rather than reassigned, for flavour and to justify uneven licence friction. Not a simulated historical event log.\n4. **Non-Kepler jurisdiction.** Off-Earth bodies (once Era 1 opens them) start with no nation ownership — jurisdiction there is corp-claim-based, not nation-based; this item only needs to state that boundary, not design the space-claim system (that's Era 1 scope, BL-087-adjacent).\n\n**Explicitly out of shape (defer further):** exact tax/licence formulas, sentiment decay/drift constants, war trigger conditions, the space-claim model. Those are sub-decisions for when this item is actually promoted, not now.\n\nAuthority docs/generation/NATION_GENERATION.md. Companion to BL-053 (which this builds on) and BL-087 (Era 1, for the non-Kepler boundary).\n\n## SETTLED 2026-08-02 — three of the four parts have moved out. What remains is small and still parked.\n\nToday's design sweep settled BL-155, BL-158 and BL-218, and between them they absorb most of\nthis item. Re-stating the scope so it is not built twice:\n\n| The 2026-07-08 shape | Where it lives now |\n|---|---|\n| **1. Nation system** — tax, licence gate, infrastructure scalar, war flag | **Split.** Tax is a law (BL-155 family a, margin modifier). The licence gate is a law (family d, gate). Only the **infrastructure scalar** and the **war flag** are genuinely new and stay here. |\n| **2. Sentiment graph** | **BL-158.** Settled today as asymmetric per-pair `sentiment[from][to]`. Delete from this item. |\n| **3. Historical fragmentation** | **BL-218.** Folded in today — a real settlement sim produces exclaves and disputed zones for free, rather than decorating a Voronoi output with them. |\n| **4. Non-Kepler jurisdiction** | **Stays**, as a boundary statement only (unchanged). |\n\n### What this item actually is now\n\n**The runtime nation actor** — the per-tick behaviour that *applies* the laws BL-155 defines,\n*issues* licences, carries the infrastructure scalar, and holds the war flag. That is nation AI,\nwhich the standing rules prohibit beyond the data-model minimum, so it stays parked and out of\nscope until BL-094 (v0.2.0) makes the nation a strategic actor.\n\nThe distinction now sharp enough to state in one line: **BL-155/BL-158 give nations things to\nHAVE; this item gives them things to DO.** The having is designed; the doing is parked.\n\n**Difficulty 5 -> 3.** Two thirds of the original scope is elsewhere. It also no longer needs\nbreaking down, which a difficulty 5 would have required.\n\n**Stays parked (F).** `requires` records BL-155 and BL-158, since it applies what they define.\n\n## Territorial half REOPENED 2026-08-03 (Ben, NR-036)\n\nBL-218 folded this item's territorial-fragmentation half in on the argument that a real\nsettlement sim produces exclaves for free. The argument was never measured. Ben's call: the\nterritorial half is **not complete** and is reopened as **BL-284** (exclave measurement), which\ncounts non-contiguous nation components and attributes them (emergent vs Pass 2b orphan\nassignment). Do not mark this half complete until BL-284 closes.\n\nCLOSED 2026-08-07 settled-in-design (cut audit, Ben approved). Settled 2026-08-02 by redistribution: tax/licences to BL-155, sentiment to BL-158, fragmentation to BL-218 then BL-284. The runtime nation actor residual is v0.3.0 governing-body work that BL-094 will re-specify from the new player identity."
    },
    "BL-230": {
      "design": "Ben (2026-07-30): \"make sure tooltips do not stick when initially open, and take 1/2 a second before they appear, then 2 seconds before they stick. From cursor pov they are behind tiles until they stick, but from a rendering pov they are always in front.\"\n\n**Landed.** Splits BL-228's single freeze-on-summon hover card into two phases: a GLANCE (kHoverAppearDelay = 30 frames @ 60 Hz = 0.5 s) that tracks the live cursor like an ordinary tooltip, then a STUCK freeze (kHoverStickDelay = 150 frames total = 2.5 s, i.e. 2 s after appearing) that stops tracking and is dismissed only once the pointer leaves its bounds + the existing exit pad. The window keeps ImGuiWindowFlags_NoInputs and the Tooltip flag throughout both phases, so it always draws above every other window (rendering pov) but never captures the pointer (cursor pov) — canvas hover/click always resolves to whatever tile or marker is beneath it, in either phase; nothing new was needed here since NoInputs already gave that property.\n\nscripts/verify/hover_freeze.lua rewritten from 3 to 5 captures to exercise both phases (glance-appears, glance-tracks-cursor, stuck, pinned-after-nudge-while-stuck, dismissed-on-exit); goldens re-blessed.",
      "resolution": "COMPLETE 2026-07-31 (closed out). The hover glance-then-stick behaviour landed earlier and ROADMAP § v0.1.1 already records it as \"already landed\", but the item carried status \"landed\" -- not a terminal status per backlog_lint (TERMINAL = complete|shipped) -- and no resolution note, so it still counted as open work in every status query.\n\nVerified present by re-running scripts/verify/hover_freeze.lua on a Linux build: all five captures render (glance tracks cursor, stuck, pinned after nudge, dismissed on exit)."
    },
    "BL-287": {
      "design": "MEASURED, NOT SUSPECTED (2026-08-04). A full verify-tier rebuild was taking 45-90 minutes and\nblocking a session. The cause is one line: CMakeLists.txt:433\n\n    add_executable(${harness_name} \"${harness_src}\" ${IO_WORLD_SOURCES})\n\nEvery harness target compiles the ENTIRE world layer from source. With 44 harnesses in\ntools/verify/ and 30 world TUs in IO_WORLD_SOURCES, that is 1,320 compilations of the same 30\nfiles per clean build. The object files are byte-identical across all 44 targets; only the\nharness TU differs.\n\nTHE FIX. Compile the world layer once as an OBJECT library and link every harness against it:\n\n    add_library(io_world_obj OBJECT ${IO_WORLD_SOURCES})\n    target_include_directories(io_world_obj PUBLIC src)\n    # per harness:\n    add_executable(${harness_name} \"${harness_src}\" $<TARGET_OBJECTS:io_world_obj>)\n\nThat takes the world-layer compilation count from 1,320 to 30 — a 44x reduction on the\ndominant cost. Harness TUs (44) stay as they are. Nothing about the harnesses themselves\nchanges; this is purely how the same objects are produced.\n\nSECOND, SMALLER WIN. target_precompile_headers is applied only to ProjectIo (CMakeLists.txt:153),\nso the harness tier — which is the overwhelming majority of the compiling — gets no PCH benefit.\nThe OBJECT library is the natural place to attach one.\n\nADJACENT, NOT PART OF THIS ITEM. The default build/ directory is configured NMake Makefiles +\nDebug, which is single-threaded (15 of 16 cores idle) and produces harnesses that run ~30x\nslower than Release. A build_rel/ directory already exists configured Ninja + Release and is\nsimply not the default anyone reaches for. Worth a line in DEVELOPMENT_PRACTICES pointing at\nit; that is documentation, not this code change.\n\nWHY IT IS WORTH DOING. This is not a micro-optimisation: it is the difference between a\nverify-tier rebuild being a coffee break and being a session-ending event. Any change to a\nwidely-included header (components.hpp, world.hpp) currently invalidates all 1,320. The\nBL-286 resource_type extension did exactly that and cost this session most of an hour.\n\nVERIFICATION. Time a clean build of the verify tier before and after, and confirm ctest still\npasses the same set. The object-library form must produce identical binaries — if any harness\nbehaves differently, something was depending on per-target compile flags and that is the real\nfinding.",
      "resolution": "LANDED 2026-08-04. src/world/*.cpp (minus the three Lua TUs) is now the OBJECT library\nio_world_obj, compiled once. The 44-harness foreach and both Lua harnesses\n(pregame_balance, persona_counsel) link it instead of listing ${IO_WORLD_SOURCES} as their\nown sources. Include dir and cxx_std_20 are PUBLIC so consumers inherit them by linking;\nIO_WARNING_FLAGS stays PRIVATE so it applies to the world layer and does not leak onto a\nconsumer's TU. World-layer compilations per clean build: 1,320 -> 30.\n\nFull-tier incremental rebuilds now run in 1.5-6.5 s. A true from-cold number was NOT\nmeasured: a fresh configure currently fails downloading SDL3 (see NR-048), so verification\nreused build_rel, whose deps are cached.\n\nIT ALSO EXPOSED A LATENT CRASH, which is exactly what this item said to watch for. Changing\nlink order made econ_bankruptcy segfault: its resource_name guarded with idx < resource_count\nwhile its name table holds 19 entries, so it had been reading out of bounds since\nresource_count was 23. BL-286 widened it to 31, the overrun grew to 12 slots, and the new\nlayout put that on unmapped memory. Fixed by guarding on the table extent.\n\nChecking for the same shape elsewhere found ui/presentation.cpp: resource_table is declared\n[resource_count], so widening the enum zero-fills the new rows, making resource_name() return\na NULL pointer that every caller passes to \"%s\". Not reachable today only because callers\nguard on a positive quantity and nothing produces the new goods. presentation_of now returns\na placeholder rather than a null. Names and colours for the eight goods stay unauthored,\ndeliberately - that is design work belonging with BL-287-290 behaviour.\n\nctest: 40/44 before and after, same four failures (BL-288). No behaviour change beyond the\nsegfault fix."
    },
    "BL-294": {
      "design": "FOUND BY THE DOCUMENTATION AUDIT (2026-08-04). Two symbols survive with no callers:\n\n1. `diverging_colour` — zero call sites. It went dead when the Production lens moved to\n   `production_colour` (red -> yellow -> green). LENSES.md still describes Production as using\n   \"the same band the Market wash uses\", which is how the deletion stayed invisible.\n\n2. `icons::unit` — zero call sites. ICONS.md's catalogue entry claims canvas use under\n   \"Drawn for / where\"; nothing draws it.\n\nDELETE BOTH, and correct the two doc claims that made them look live. The icon is the one worth a\nsecond's thought: units are coming back under BL-157 (military datamodel stub), so either keep the\nglyph and mark it explicitly unused-pending-BL-157, or delete it and redraw then. Prefer deleting\n— a glyph with no caller cannot be verified, and ICONS.md's own rule is that the code is the\nsource of truth.",
      "resolution": "Landed 2026-08-09. diverging_colour deleted (lived in body_surface_canvas.cpp, not presentation.hpp as filed) and icons::unit deleted — both grep-confirmed zero callers incl. post-BL-324 unit work; LENSES.md Production/Market-wash claims and ICONS.md catalogue corrected."
    },
    "BL-295": {
      "design": "FOUND BY THE DOCUMENTATION AUDIT (2026-08-04). The comment block on the logistics-goods block of\n`resource_type` (components.hpp:60-71) names BL-287, BL-288, BL-289 and BL-290 as the follow-on\nitems that will add consumption, range-cap, shelf-life and purchase behaviour.\n\nNONE OF THOSE IDS MEANS THAT. BL-287 is the verify-tier build change; BL-288 is the Release-only\ntest failures; BL-289, BL-290 and BL-291 did not exist when the comment was written. BL-286's own\ndesign correctly said \"(not yet filed)\" for each behaviour — the caveat was dropped when the\nprose was transcribed into the comment and into RESOURCES.md.\n\nRESOURCES.md has been corrected (2026-08-04). This item is the code half.\n\nTHE FIX IS ONE COMMENT: say the follow-ons are unfiled, and that ids get allocated with\n`node tools/session/next_id.js` when they are. Do not invent placeholder ids — a wrong id is\nworse than no id, because it routes the reader to real but unrelated work. That is the whole\nlesson here and it is worth stating in the comment itself.",
      "resolution": "Landed 2026-08-09. components.hpp logistics-goods comment rewritten: follow-ons stated unfiled, ids via next_id.js when filed, placeholder ids worse than none."
    },
    "BL-307": {
      "design": "FILED AND DELIVERED 2026-08-04 (Ben, during the BL-296 constellation review: every node must be recorded in some form; backlog then deliver). docs/research/ancient_tech_ladder.json is the canonical machine form of ANCIENT_TECH_LADDER.md tables — 58 techs + 6 roster regimes across six bands x seven domains, 8 vertex quests, 3 keystones, 2 worked crossings. Conventions: gate = array of endowment atoms or null; diffusion = array of classes or null for regimes; quest conditions are typed atoms (research / market / structure / surplus / keystone) with the doc placeholder thresholds (X, N) kept until tuning; provenance marks the authoring pass (ladder / neighbourhood-pass / density-slice). tools/session/ladder_lint.js checks id shape, prereq resolution + band ordering, vocabulary membership, quest/keystone reference resolution, and a two-way id cross-check against the doc — it caught the neighbourhood count drift (19 techs / 27 objects, prose said 20 / ~28; both fixed). RELATION TO OPEN QUESTION 3 (data home, C++ vs Lua): this store is research-phase and does not pre-commit the sims consumption format; whichever home wins transcribes from here. Schema calls taken on Bens behalf recorded in NR-062."
    }
  }
}
