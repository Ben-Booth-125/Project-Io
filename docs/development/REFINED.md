# REFINED — active worklist

**Sprint 39 — the drama of the time-lapse.** Promoted 2026-09-11 for Batch Delivery. One task block per
item; each task is scoped to the item's declared files (backlog.json) and satisfies the rows of its
requirement group (`req/requirements.json`, batch `sprint-39-drama`). Worktree agents build and commit on
their branch; the main session merges, builds, sweeps and live-clicks.

## Waves

- **Wave 1 (baseline + independent): DONE 2026-09-11.** BL-926, BL-927, BL-918, BL-919, BL-915,
  BL-916, BL-922 — built, verified, re-blessed and archived; see the DEVLOG entry "Sprint 39 wave 1
  closes out" and `docs/development/archive/backlog-design-2026-Q3.json` / `archive/requirements-
  2026-Q3.json` for the closed rows.
- **Wave 2 (dependents):** BL-914, BL-917, BL-920, BL-921, BL-924, BL-925, BL-929.
- **Wave 3:** BL-923.

## BL-914 - the pass rounds render while they compute

- **Task 1 — build the item as designed** (`node tools/session/backlog_query.js --grep BL-914 --full`). Files: the item's declared list. Satisfies: R1. Status: pending.
- **Task 2 — verify and measure**: run the rows' verification classes; sim items read history_sweep before and after against the wave-1 baseline. Status: pending.

## BL-917 - roads grow on the map and cross rivers as bridges

- **Task 1 — build the item as designed** (`node tools/session/backlog_query.js --grep BL-917 --full`). Files: the item's declared list. Satisfies: R1. Status: pending.
- **Task 2 — verify and measure**: run the rows' verification classes; sim items read history_sweep before and after against the wave-1 baseline. Status: pending.

## BL-920 - the phase opens on culture ground and city states rise by organising it

- **Task 1 — build the item as designed** (`node tools/session/backlog_query.js --grep BL-920 --full`). Files: the item's declared list. Satisfies: R1, R2. Status: pending.
- **Task 2 — verify and measure**: run the rows' verification classes; sim items read history_sweep before and after against the wave-1 baseline. Status: pending.

## BL-921 - a pooled levy along the supplied network

- **Task 1 — build the item as designed** (`node tools/session/backlog_query.js --grep BL-921 --full`). Files: the item's declared list. Satisfies: R1. Status: pending.
- **Task 2 — verify and measure**: run the rows' verification classes; sim items read history_sweep before and after against the wave-1 baseline. Status: pending.

## BL-924 - a region is worth attacking for what stands on it

- **Task 1 — build the item as designed** (`node tools/session/backlog_query.js --grep BL-924 --full`). Files: the item's declared list. Satisfies: R1. Status: pending.
- **Task 2 — verify and measure**: run the rows' verification classes; sim items read history_sweep before and after against the wave-1 baseline. Status: pending.

## BL-925 - amicable neighbours trade across the border

- **Task 1 — build the item as designed** (`node tools/session/backlog_query.js --grep BL-925 --full`). Files: the item's declared list. Satisfies: R1, R2. Status: pending.
- **Task 2 — verify and measure**: run the rows' verification classes; sim items read history_sweep before and after against the wave-1 baseline. Status: pending.

## BL-929 - a polity upgrades supply sites from its stockpile

- **Task 1 — build the item as designed** (`node tools/session/backlog_query.js --grep BL-929 --full`). Files: the item's declared list. Satisfies: R1, R2. Status: pending.
- **Task 2 — verify and measure**: run the rows' verification classes; sim items read history_sweep before and after against the wave-1 baseline. Status: pending.

## BL-923 - over-extended ground breaks into city states

- **Task 1 — build the item as designed** (`node tools/session/backlog_query.js --grep BL-923 --full`). Files: the item's declared list. Satisfies: R1, R2. Status: pending.
- **Task 2 — verify and measure**: run the rows' verification classes; sim items read history_sweep before and after against the wave-1 baseline. Status: pending.

