# Project Io — REFINED (active worklist)

*Empty. Sprint 17b (the shell stops fighting the map) closed 2026-08-24 with all nine items
complete; its plan and retro are in [`SPRINTS.md`](SPRINTS.md) and `sprints.json`.*

**Open work with no sprint**, so it is not lost between blocks:

- **NR-606** — four verify scripts fail on causes that predate 17b: `sticky_card` and
  `building_element` on a stale fixture, `battle_card` on a no-argument call in a script that has
  never run, and `recipe_workforce` on a building at 100% workforce producing **nothing**. The last
  is an economy finding wearing a UI check's clothes and is the one worth taking first.
- **NR-604** — `LENSES.md`'s rung table describes three surfaces that do not exist (Scarcity's
  Circumplanetary badge, Reach's and Supply-routes' Solar rungs). Authority docs are
  state-independent, so those are **work or a ruling**, not doc errors.
- **NR-603** — retiring the Opportunity lens retired BL-086's ambient growth read with it. Getting
  a glanceable "where is demand unmet" back is new work, not a rediscovery.
- **NR-605** — the Selection band now carries two centre-column idioms: an accordion for the tile,
  pagers for every other kind.
- **NR-593** — the band's resting state is ~90% empty. The accordion shape from BL-598 makes it
  cheap; what it needs is Ben naming the sections, because naming them is the design.
