# Next session

**Sprint 42 is open** — *generation sharpened before Digitisation* (`docs/development/sprints.json`,
id 42). **Wave 0 is in flight.** Read `docs/development/REFINED.md` § Sprint 42 for the promoted tasks
and requirement groups, then take items one at a time with
`node tools/session/backlog_query.js --grep BL-9xx --full`.

Mode is set by the item, not by this note (`CLAUDE.md` § 1). The sprint's goal, risk, done-when
and sequence are on its row in `sprints.json`; `node tools/session/render_sprints.js` mirrors them.
