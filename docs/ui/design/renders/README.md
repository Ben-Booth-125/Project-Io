# Example Renders

Composite "finished game" screenshots for the ST1 style-sheet sprint — UI, map,
icons, tiles etc. put together as one image so the visual direction can be judged
as a whole rather than piece by piece.

Naming: `iter-01.png`, `iter-02.png`, ... one per iteration, oldest first. Log each
one in the Iterations table in `../GLOBAL_STYLE_SHEET.md`.
