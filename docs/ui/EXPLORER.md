# Project Io — Explorer (superseded)

> **Superseded 2026-07-26 (BL-205).** The Explorer was a specified-but-never-implemented
> placeholder — a pinning / quick-navigation surface reserving the right middle shell band.
> That band went to the **comms chat log** (`CHAT.md`, `src/ui/chat_panel.{hpp,cpp}`), the
> surface of the diplomacy-as-communication principle (`docs/ai/AI_OPPONENT.md` § 7); the
> comms log has since moved to the **bottom-left dock** (BL-227, 2026-07-30), and the right
> middle band is canvas again.
>
> Pinning was never wired (the `highlight.hpp` `pinned` tier and the amber palette slot
> remain unused). If a pinning affordance returns, it will be designed fresh — likely
> chat-adjacent or Selection-driven — rather than reviving this reserved band. This file
> is retained as the supersession record; the original spec is in git history.
