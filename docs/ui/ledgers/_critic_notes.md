# Cross-doc consistency critic notes

**⚠ THIS CERTIFICATION IS VOID — the mock data was re-blessed after it was written (2026-08-04).**
Every figure below is stale, and because this note *certified* them, it launders the same stale
numbers into `balance.md`, `market.md`, `economy.md` and `tile_ledger.md`. Do not cite it.

What actually changed, spot-checked 2026-08-04: `markets.csv` now carries a **`market_id` column**
(the gap the Market doc filed as a fix has shipped) and holds **4 markets over 68 rows**, not the
"5× fan-out, 35/7" this note certified. `cashflow.csv` no longer has 30310 as its first row and the
`net -173.83` figure is gone. `player_timeseries` opens at balance **8512.72**, not 1963.13.

**The lesson, not the numbers.** A verification note that pastes measured values ages the moment the
fixture is re-blessed, and it ages *invisibly* — worse than no note, because six sibling docs cited
it as authority. If this is rewritten, it should record the *method* (which file, which column,
which invariant) and leave the reader to run it. Goldens are disposable; a note that hard-codes
their contents is not.

*(The original text is kept below for the reasoning it carries about overlap and lens pairing,
which does not depend on the figures.)*

~~MOCK-DATA VERIFICATION (all checked against docs/ui/mockdata/*.csv): markets.csv has 35 data rows / 7 resources = 5 markets all on body_id 15124 with no market_id column — the "5x fan-out, 35/7" claim is ACCURATE (Market, Tile Ledger docs correct). stockpiles.csv = 1 row (FararAthar Iron Ore 16, NOT Genom) — "sparse ~1-2 rows" ACCURATE. workforce.csv uniform 85.4724 — ACCURATE. player = 30310 Genom Systems — ACCURATE. cashflow.csv row 30310: income 16.0007, exp 159.992, maint 20, wages 4.69095, interest 5.14972, net -173.832 — Balance doc's "interest 5.15, net -173.83" ACCURATE. player_timeseries tick0 balance 1963.13 -> tick15 -262.636 — Economy's "+1963 to -262 over 16 ticks" ACCURATE. corporations.csv starting_capital=0 / since_start=balance dup for all 8 corps — ACCURATE everywhere cited. All mock-data claims across all seven docs verified correct. One minor: Corporation doc says "8 corps in mock" — confirmed 8 (30304-30311), correct.~~

LENS SANITY: all lens names cited exist in overlay_mode (src/ui/ui_state.hpp: none/supply/market/country/corporation/resource/population/opportunity/production/scarcity/industry). BUT one lens is MISCHARACTERIZED: Economy §3 claims Industry is "the on-canvas twin of the whole-economy sector-balance (extraction/processing distribution across bodies)". Per LENSES.md and ui_state.hpp, overlay_mode::industry is the per-tile NATION-SUBSTRATE throughput field (the AI-owned broad economy, occupation x terrain richness) — it does NOT paint the player's corp building mix. Arming Industry for a "my sector footprint" view would show the nation substrate, not the player's extraction/processing split. This is the one genuinely wrong lens pairing in the set. (Also note Industry is keyboard-cycle-only off the visible bar per BL-093 — arming it programmatically is fine but it's not strip-selectable.)

CROSS-DOC OVERLAP (the aggregate-only boundary): The Economy doc is the epicentre of the overlap risk and does not fully honour "aggregate-only, no overlap with Corporation or Market." Three specific collisions: (1) Economy "Standing" (corp balance rank, player pinned) vs Corporation "Standings" (corp balance table, player tinted) — same question "where do I rank vs rivals"; Economy acknowledges and tries to differentiate (rank ladder vs full table) but the line is thin and BOTH default-open to this question. (2) Economy "Cashflow" vs Balance "Cashflow" — IDENTICAL: same cashflow.csv, same income/expenditure/maintenance/wages/interest/net lines, same player corp 30310. Economy calls it "aggregate where-does-money-go" but at the player level it is byte-for-byte the Balance Cashflow tab. Economy does NOT flag this collision (it flags Holdings/Markets overlap but misses its own Cashflow == Balance Cashflow). (3) Building-composition appears in THREE docs: Economy "Sector" (extraction/processing tally), Corporation "Profiles" (assets.size() count), Balance "Assets" (asset count/roll-up). Balance's open-Q correctly flags the Corporation duplication; Economy's Sector is defensibly distinct (aggregate composition bar, not a per-corp list) but the boundary is unstated. Net: the money/standing surface is split across Balance + Economy + Corporation without one coherent rule. Recommend the docs converge on: Balance = MY money detail (solvency/cashflow/runway, per-corp via combo); Corporation = RIVAL FIELD ranking + profiles; Economy = strictly aggregate cross-cutting roll-ups that are neither a single corp's ledger nor a single market's board (i.e. Economy must NOT re-answer "my cashflow" or "corp balance rank" — those are Balance and Corporation). Economy's own open-Q (A vs B: keep Economy as 4th rail ledger, or fold into Corporation) is the right escape hatch and should be foregrounded, because as drafted Economy substantially duplicates Balance+Corporation.

CLEAN OVERLAP HANDLING (no fix needed): Tile Ledger correctly flags its Market view duplicates the Market ledger and recommends dropping it (clean). Construction correctly flags Sell Orders as a candidate to move to Market, and Manage-vs-Selection as a real column-tenant collision (clean, well-surfaced). Selection correctly stays out of the toggle rule (selection-driven, no rail slot) and correctly notes it is mutually exclusive with ledgers, column-hosted, no rail slot, content-relayout deferred to BL-123 — fully compliant with the settled selection decision.

TOGGLE RULE: applied consistently and correctly across all six rail-ledger docs (Corporation, Balance, Market, Construction, Economy, Tile Ledger) — each states the slot icon toggles open/closed, re-clicking the ACTIVE tab closes the ledger (not collapse-to-overview), switching tabs is an ordinary view change, and in-view/cross-cutting selectors are toggle-exempt. Selection correctly declares the rule does NOT apply to it. No inconsistency found. Two docs correctly note their special cases: Economy (no rail slot today — rule has nothing to fire it) and Tile Ledger (still floating, has an ImGui titlebar close, migration deferred BL-119).

TEMPLATE ADHERENCE: all seven follow the 5-axis template (top question / sub-levels+default / lens / data gaps / close semantics / open questions) plus the working-doc header. Selection legitimately adapts axis 2 (polymorphic-by-kind rather than tabbed views) and axis 5 (selection-driven not toggle) — deviations are correct for its nature, not template violations.

LENS "USUALLY ARM" PREFERENCE: honoured everywhere. Only two docs arm none-by-default and both justify it correctly: Balance (money has no spatial expression) and Selection (feedback-loop risk with the lens-drives-resolution rule). Good judgement, consistent with the preference being "usually" not "always".
