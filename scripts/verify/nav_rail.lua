-- Verify the nav-rail slot order and placeholder glyphs (BL-021 / menu-items-from-systems R1).
-- Captures the default Solar canvas with all panels closed so the nav rail is
-- clearly visible showing all 9 slots.
verify.capture("nav_rail_default")
