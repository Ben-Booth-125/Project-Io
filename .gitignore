# Build output
build/
build-web/
build_gen/
build_live/
build_rel/
build_vs/
out/
dist/
.cmake/

# Runtime-generated user settings (BL-076 display options)
options.cfg

# Backups
backups/
generic files/

# CMake generated
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.cmake
!CMakeLists.txt

# IDE
.vs/
*.user
*.suo
.vscode/launch.json

# Claude Code personal/project-local settings
.claude/settings.local.json

# Dear ImGui auto-saved window state (per-machine runtime layout)
imgui.ini

# Verify-harness capture output (run-from-repo-root lands here; goldens under
# scripts/verify/golden/ are the committed references)
screenshots/

# Build and harness logs
*.log

# Compiled artifacts
*.exe
*.pdb
*.ilk
*.obj
*.o
*.lib
*.a
*.dll
*.so

# FetchContent cache — normally inside build/ (covered above), but the local dev
# setup points FETCHCONTENT_BASE_DIR at the repo root so a re-configure does not
# re-download ~118 MB of SDL/Lua/sol2/ImGui sources.
_deps_cache/

# Local launch convenience scripts (machine-specific absolute paths).
_run.bat

.claude/worktrees/
tools/session/.timer_state
